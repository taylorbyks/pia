import { Token } from '../types'

const parser = (tokens: Token[]) => {
  // Should use LR(1) parser here which create a tree-like intermediate representation that depicts the grammatical structure of the token stream
}

export { parser }
